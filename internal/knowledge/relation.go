package relation

import (
	"github.com/cortexa-llc/ai-pack/internal/knowledge"
)

// Relation related functionality here...

import (
	"errors"
)

// CreateRelation creates a relation and validates its type.
func CreateRelation(relType string) error {
	if err := validateRelType(relType); err != nil {
		return err
	}
	// Existing implementation here...
}

// DeleteRelation deletes a relation and validates its type.
func DeleteRelation(relType string) error {
	if err := validateRelType(relType); err != nil {
		return err
	}
	// Existing implementation here...
}

// TraverseRelations traverses relations and validates their type.
func TraverseRelations(relType string) error {
	if err := validateRelType(relType); err != nil {
		return err
	}
	// Existing implementation here...
}func CreateRelation(relType string) error {
	// Validate relType before creating a relation
	if err := validateRelType(relType); err != nil {
		return err
	}

	// Existing implementation here...
}

	// Validate relayType before creating a relation
	if err := validateRelType(relType); err != nil {
		return err
	}
}

func DeleteRelation(relType string) error {
	// Validate relType before deleting a relation
	if err := validateRelType(relType); err != nil {
		return err
	}

	// Existing implementation here...
}

	// Validate relayType before deleting a relation
	if err := validateRelType(relType); err != nil {
		return err
	}
}

func TraverseRelations(relType string) error {
	// Validate relType before traversing relations
	if err := validateRelType(relType); err != nil {
		return err
	}

	// Existing implementation here...
}

	// Validate relayType before traversing relations
	if err := validateRelType(relType); err != nil {
		return err
	}
}// Calls to validate relType should be added in CreateRelation, DeleteRelation, and TraverseRelations functions.// // This package handles relationships for the knowledge base system.
// This package handles relationships for the knowledge base system.
package knowledge

import (
	"github.com/cortexa-llc/ai-pack/internal/knowledge"

	"errors"
)


import (
	"github.com/cortexa-llc/ai-pack/internal/knowledge"
)

import (
	"github.com/cortexa-llc/ai-pack/internal/knowledge"
)

import (
	"github.com/cortexa-llc/ai-pack/internal/knowledge"
)

import (
	"github.com/cortexa-llc/ai-pack/internal/knowledge"
)

import (
	"github.com/cortexa-llc/ai-pack/internal/knowledge"
)

import (
	"github.com/cortexa-llc/ai-pack/internal/knowledge"
)
// The package relation

import (
	"github.com/cortexa-llc/ai-pack/internal/knowledge"
) was mistakenly importing itself in its relation file. This was corrected to avoid cyclic imports.
package relation

import (
	"github.com/cortexa-llc/ai-pack/internal/knowledge"
)

import (
	"github.com/cortexa-llc/ai-pack/internal/knowledge"
))


import (
	"github.com/cortexa-llc/ai-pack/internal/knowledge"
)
    err := validateRelType(relType)

