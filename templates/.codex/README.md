# Codex Optional Assets

This directory contains optional Codex-specific guidance you can reference from
`AGENTS.md`. It is safe to customize files here in your project.

Recommended use:
- Add additional rules in `.codex/rules/`
- Link those rules from `AGENTS.md` under "Project-Specific Rules"
